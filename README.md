# Machine Test

A complete full-stack application with React TypeScript frontend and Express.js backend for contact management and dashboard analytics.

## 🚀 Quick Start

### Prerequisites

- **Node.js** (v16 or higher)

### 📥 Installation & Setup

#### Method 1: Quick Setup (Recommended)

```bash
# 1. Clone the repository
git clone <your-repository-url>
cd machine_test

# 2. Install backend dependencies
cd backend
npm install

# 3. Install frontend dependencies
cd ../frontend
npm install

# 4. Start both services (in separate terminals)
# Terminal 1 - Backend
cd backend
node server.js

# Terminal 2 - Frontend
cd frontend
npm run dev
```
